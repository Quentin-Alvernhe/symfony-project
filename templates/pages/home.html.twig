{% extends 'base.html.twig' %}

{% block body %}
    <div class="text-center mt-5">
        <h1>Bonjour, bienvenue sur le gestionnaire go-med</h1>

        <a href="{{ path('app_user_index') }}" class="btn btn-primary mt-4">Voir les utilisateurs</a>
        <a href="{{ path('app_activite_index') }}" class="btn btn-primary mt-4">Voir les infos abonnés</a>
        <a href="{{ path('app_syndicat_index') }}" class="btn btn-primary mt-4">Voir les syndicats</a>
    </div>
{% endblock %}